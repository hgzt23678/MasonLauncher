# Mason Launcher 作業メモ

## プロジェクト

- アプリ名: Mason Launcher
- npm識別子: `mason-launcher`
- 目的: Minecraft: Java Edition向けの非公式インスタンスランチャー
- 構成: Electron 42 / Vite / TypeScript / Material Web
- UI: フレームワークなしのDOM操作

## 基本方針

- Vanilla起動経路を壊さない。
- 認証回避、任意ユーザー名、所有権未確認での起動を実装しない。
- Minecraft、Forge、libraries、assets、MODを同梱しない。
- Microsoft/Minecraftのtokenをログへ出さない。
- rendererからNode.js APIを直接使用しない。
- ファイル操作、認証、プロセス起動、OS依存処理はmain processに置く。
- rendererとの通信はpreloadの`contextBridge`を経由する。

## 主要モジュール

- `src/main.ts`: Electron起動、設定、IPC、起動ワークフロー
- `src/auth-service.ts`: Microsoft / Xbox / XSTS / Minecraft Services認証
- `src/minecraft-service.ts`: ダウンロード、Java、Forge、起動処理の統合
- `src/minecraft-downloader.ts`: 公式メタデータとゲームファイルの取得・検証
- `src/minecraft-launch-resolver.ts`: classpath、JVM/game引数、mainClassの解決
- `src/minecraft-process-runner.ts`: Javaプロセスとログ、終了状態の監視
- `src/forge-service.ts`: Forge installer、libraries、processorsの処理
- `src/java-runtime-service.ts`: Java/JRE検出、選択、管理
- `src/modrinth-service.ts`: Modrinth APIとMOD同期
- `src/offline-auth-cache.ts`: 所有権確認済みアカウントのオフライン認可
- `src/diagnostics.ts`: ログ収集と秘密情報のマスキング

## 永続データ

- 現行設定: `%APPDATA%\Mason Launcher\launcher-settings.json`
- 現行管理runtime: `%APPDATA%\Mason Launcher\runtime\`
- 新規インスタンス: `<gameDirectory>\mason-launcher\profiles\<profileId>\`
- 旧アプリ名のuserDataは初回起動時に現行ディレクトリへ移行する。
- 保存済み`instanceDir`はセーブデータ保護のため変更しない。
- `.simple-craft-*`形式の隠しマーカーは既存環境との互換性のため維持する。

## 注意点

- `undici`は`7.27.2`固定。変更時は`src/xmcl-compat.ts`も検証する。
- Forge installerをMinecraft client.jarとして扱わない。
- Forge processor用classpathとゲーム起動用classpathを混同しない。
- オフライン起動は正規認証・所有権確認済みキャッシュが有効な場合だけ許可する。
- `productName`変更はElectronの`userData`パスを変更するため、移行処理を削除しない。
- `Modrinth`は外部サービス名として使用する。アプリ自身の名称には使用しない。

## 検証

変更後は最低限、次を実行する。

```powershell
npm run typecheck
npm run lint
npm test
npm audit --omit=dev
git diff --check
```

配布物に影響する変更では追加で実行する。

```powershell
npm run package
```

期待されるWindows出力:

```text
out\Mason Launcher-win32-x64\Mason Launcher.exe
```

## 2026-06-13 作業メモ

- `@xmcl/core`の`minecraft-window-ready`はOSウィンドウ検出ではなく、stdoutの初期化ログ検出として扱う。
- Windowsでは起動Java PIDと子孫PIDに限定して、可視・非最小化・画面内のトップレベルウィンドウを確認する。
- 実ウィンドウ未確認のコード0終了は`window-unverified`として正常終了扱いしない。
- 起動ログへ初期化トリガー行、ウィンドウプローブ結果、stdout/stderr/latest.log末尾を保存する。秘密情報は必ずマスクする。
- Javaインストールはアーカイブをストリーム保存し、SHA-1/SHA-256とサイズを検証してから展開する。
- 前回中断された`.staging-*`/`.download-*`は次回インストール前に削除する。実環境では不完全な展開残骸が2件見つかった。
- Foojay/BellSoftの実配布メタデータはSHA-1を返す場合があるため、SHA-256だけに限定しない。
- Electron renderer sandboxはmain processからspawnしたJavaを直接制限しない。`windowsHide`はコンソールウィンドウだけを隠す。
- 2026-06-13の実機調査では、Nodeの`windowsHide: true`がLWJGL/Windows Formsの最初のGUIウィンドウも`visible=false`にした。MinecraftはWindowsで`javaw.exe` + `windowsHide: false`を使う。
- 画面表示成功は同一PID/handleの可視トップレベルウィンドウが2秒継続した場合だけ確定する。
- Forge 1.12.2 (`14.23.5.2864`) は保存済み正規認証キャッシュで実起動し、可視ウィンドウ継続確認まで成功した。
## 2026-06-13 MOD loader / Modrinth

- Profile loaders: `vanilla | forge | neoforge | fabric`.
- Fabric builds and profiles use `https://meta.fabricmc.net/v2/versions/loader/...`.
- NeoForge builds and installers use `https://maven.neoforged.net/releases`.
- Resolved IDs: Forge `<mc>-forge-<loader>`, Fabric `<mc>-fabric<loader>`, NeoForge `neoforge-<loader>`.
- Modrinth searches are locked to the profile Minecraft version and loader. Empty queries use `index=downloads`; text queries use `index=relevance` and title/slug matching.
- Modrinth downloads are verified and written to the selected instance's `instance/mods` directory.
- Verified with `npm run check`, `npm run package`, live Fabric/NeoForge metadata, and live Modrinth popular search.

## 2026-06-13 Fabric / NeoForge launch matrix

- Official sources:
  - Mojang: `https://piston-meta.mojang.com/mc/game/version_manifest_v2.json`
  - Fabric: `https://meta.fabricmc.net/v2/versions/loader/<minecraft-version>`
  - NeoForge: `https://maven.neoforged.net/releases/net/neoforged/neoforge/maven-metadata.xml`
  - Temurin: `https://api.adoptium.net/v3/assets/latest/<java-major>/hotspot`
- Verified visible Minecraft windows with the spawned Java PID tree:
  - Java 8: Vanilla 1.16.5, Fabric 1.16.5 / Loader 0.19.3.
  - Java 17: Vanilla 1.20.4, Fabric 1.20.4 / Loader 0.19.3, NeoForge 1.20.4 / 20.4.251.
  - Java 21: Vanilla 1.21.1, Fabric 1.21.1 / Loader 0.19.3, NeoForge 1.21.1 / 21.1.233.
  - Java 25: Vanilla 26.1.2, Fabric 26.1.2 / Loader 0.19.3, NeoForge 26.1.2 / 26.1.2.76.
  - NeoForge 1.16.5 is unsupported and must not be offered or tested as a valid combination.
- Launches selected managed Eclipse Temurin x64 runtimes for Java 8/17/21/25.
- Root causes fixed:
  - Temurin checksums came from stale Foojay metadata; Temurin now uses the official Adoptium API and SHA-256.
  - `@xmcl/unzip` stalled on a Temurin 8 archive entry; Windows extraction now prevalidates ZIP paths and uses the system tar extractor.
  - Modern Mojang native classifier libraries were not extracted unless `isNative` was set; matching native classifiers are now extracted.
  - `@xmcl/installer` completed NeoForge processors but did not resolve; NeoForge now runs the official installer CLI and verifies the resulting version JSON.
- Minecraft game spawn is fixed to `shell=false`, `windowsHide=false`, `detached=false`, and piped stdout/stderr.
- Modrinth profile editor performs a popular search immediately when shown, without sending `query`, using loader/version facets and `index=downloads`.
- UI lifecycle verification opened the Forge profile editor without pressing Search and confirmed the initial result card was rendered.
